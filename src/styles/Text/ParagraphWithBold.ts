import styled from "styled-components";

export const ParagraphWithBold = styled.p`
  font-size: 1.15rem;
  line-height: 1.25em;
  color: var(--gray);

  strong {
    color: var(--primary-light);
  }
`;

import styled from "styled-components";

export const Title = styled.h2`
  font-weight: 500;
  font-size: clamp(2rem, 2.5vw, 2.5rem);
  line-height: 1.5em;
  color: var(--tertiary);
  margin-top: 48px;
`;
